pub mod paquet_crafter
{
    use crate::action::action::ActionResult;
    use crate::teams::team::Team;

    pub fn craft_gfx_packet(action_result: Option<ActionResult>, teams: Vec<Team>) -> Option<String>
    {
        //gfx_pkt = format!("{}", );
        None
    }
}